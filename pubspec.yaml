name: resale_item_id_poc
description: Proof of concept for item identification and pricing

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Camera and image handling
  camera: ^0.10.5
  image_picker: ^1.0.4
  image: ^4.1.3
  image_editor_plus: ^1.0.6
  
  # Storage and persistence
  path_provider: ^2.1.1
  shared_preferences: ^2.2.2
  
  # HTTP and web services
  http: ^1.1.0
  url_launcher: ^6.2.1
  
  # Google services
  google_ml_kit: ^0.18.0
  googleapis: ^13.1.0
  googleapis_auth: ^1.6.0
  
  # JSON serialization
  json_annotation: ^4.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  json_serializable: ^6.7.1
  build_runner: ^2.4.7
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
